export type Todo = {
  todo: string;
  id: string;
  changedOn: number;
};

export type RegisterInformation = {
  email: string;
  confirmEmail: string;
  password: string;
  confirmPassword: string;
};
