export type Todo = {
  todo: string;
  uidd: string;
  changedOn: number;
};

export type RegisterInformation = {
  email: string;
  confirmEmail: string;
  password: string;
  confirmPassword: string;
};
